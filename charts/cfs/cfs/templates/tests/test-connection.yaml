apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cfs.fullname" . }}-test-connection"
  labels:
    {{- include "cfs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cfs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
