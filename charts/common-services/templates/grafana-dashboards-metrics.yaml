{{/* ============================================================================
    Grafana Dashboards for Metrics-Enabled Services

    This template creates ConfigMaps for Grafana dashboards that are conditionally
    imported based on whether the respective service is enabled.

    All dashboards are automatically provisioned when the service is enabled.
    Prometheus scrapes metrics via service annotations (prometheus.io/scrape).

    Dashboards included:
    - Elasticsearch             - Search and analytics engine
    - HAProxy                   - Load balancer / API gateway
    - Fluent Bit                - Log processor / forwarder
    - Grafana                   - Self-monitoring metrics
    - ZooKeeper                 - Coordination service
    - Prometheus                - Monitoring system self-metrics
    - Kibana                    - Log visualization UI metrics
    ============================================================================ */}}

{{/* ----------------------------------------------------------------------------
    ArgoCD Dashboard
    Condition: argo-cd.enabled = true
    Metrics: argocd_* from controller, server, repo-server
    Note: Application metrics appear when ArgoCD apps are deployed
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (index .Values "argo-cd" "enabled") }}
{{- if (.Files.Glob "dashboards/argo-cd/argocd-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "GitOps"
data:
{{ (.Files.Glob "dashboards/argo-cd/argocd-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Elasticsearch Dashboard
    Condition: elasticsearch.enabled = true
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.elasticsearch.enabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: elasticsearch-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Database"
data:
{{ (.Files.Glob "dashboards/elasticsearch/elasticsearch-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}

{{/* ----------------------------------------------------------------------------
    HAProxy Dashboard
    Condition: haproxy.enabled = true
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.haproxy.enabled) }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: haproxy-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Infrastructure"
data:
{{ (.Files.Glob "dashboards/haproxy/haproxy-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}

{{/* ----------------------------------------------------------------------------
    Fluent Bit Dashboard
    Condition: fluent-bit.enabled = true
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (index .Values "fluent-bit" "enabled") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: fluent-bit-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Logging"
data:
{{ (.Files.Glob "dashboards/fluent-bit/fluent-bit-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}

{{/* ----------------------------------------------------------------------------
    Grafana Self-Monitoring Dashboard
    Condition: grafana.enabled = true
    ---------------------------------------------------------------------------- */}}
{{- if .Values.grafana.enabled }}
{{- if (.Files.Glob "dashboards/grafana/*.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-metrics-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Observability"
data:
{{ (.Files.Glob "dashboards/grafana/*.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    ZooKeeper Dashboard
    Condition: zookeeper.enabled = true
    Metrics: JMX metrics via zookeeper-metrics service
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.zookeeper.enabled) }}
{{- if (.Files.Glob "dashboards/zookeeper/zookeeper-metrics-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: zookeeper-metrics-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Infrastructure"
data:
{{ (.Files.Glob "dashboards/zookeeper/zookeeper-metrics-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Prometheus Self-Monitoring Dashboard
    Condition: prometheus.enabled = true
    Metrics: prometheus_* self-metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.prometheus.enabled) }}
{{- if (.Files.Glob "dashboards/prometheus/prometheus-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Observability"
data:
{{ (.Files.Glob "dashboards/prometheus/prometheus-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    PostgreSQL Dashboard
    Condition: postgresql.enabled = true
    Metrics: pg_* from postgres_exporter sidecar
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.postgresql.enabled) }}
{{- if (.Files.Glob "dashboards/postgresql/postgresql-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: postgresql-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Database"
data:
{{ (.Files.Glob "dashboards/postgresql/postgresql-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Kibana Dashboard
    Condition: kibana.enabled = true
    Metrics: kibana_* from custom prometheus exporter sidecar
    Exporter: https://github.com/gnanirahulnutakki/kibana-prometheus-exporter
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.kibana.enabled) }}
{{- if (.Files.Glob "dashboards/kibana/kibana-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: kibana-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Logging"
data:
{{ (.Files.Glob "dashboards/kibana/kibana-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    OpenSearch Dashboard
    Condition: opensearch.enabled = true
    Metrics: opensearch_* cluster and node metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.opensearch.enabled) }}
{{- if (.Files.Glob "dashboards/opensearch/opensearch-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: opensearch-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Infrastructure"
data:
{{ (.Files.Glob "dashboards/opensearch/opensearch-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Velero Dashboard
    Condition: velero.enabled = true
    Metrics: velero_* backup and restore metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.velero.enabled) }}
{{- if (.Files.Glob "dashboards/velero/velero-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: velero-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Backup"
data:
{{ (.Files.Glob "dashboards/velero/velero-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Loki Dashboard
    Condition: loki.enabled = true
    Metrics: loki_* log aggregation metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.loki.enabled) }}
{{- if (.Files.Glob "dashboards/loki/*.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Logging"
data:
{{ (.Files.Glob "dashboards/loki/*.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Flink Dashboard
    Condition: flink-kubernetes-operator.enabled = true
    Metrics: flink_* stream processing metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (index .Values "flink-kubernetes-operator" "enabled") }}
{{- if (.Files.Glob "dashboards/flink/flink-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: flink-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Data Processing"
data:
{{ (.Files.Glob "dashboards/flink/flink-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}

{{/* ----------------------------------------------------------------------------
    Alloy Dashboard
    Condition: alloy.enabled = true
    Metrics: alloy cluster and pipeline metrics
    ---------------------------------------------------------------------------- */}}
{{- if and (.Values.grafana.enabled) (.Values.alloy.enabled) }}
{{- if (.Files.Glob "dashboards/alloy/alloy-cluster-dashboard.json") }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: alloy-dashboard
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: "Infrastructure"
data:
{{ (.Files.Glob "dashboards/alloy/alloy-cluster-dashboard.json").AsConfig | indent 2 }}
---
{{- end }}
{{- end }}
